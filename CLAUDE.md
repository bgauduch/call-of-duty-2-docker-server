# CLAUDE.md

The canonical, tool-agnostic instructions for this repository live in
[AGENTS.md](AGENTS.md). They are imported below so Claude Code picks them up
automatically — Claude Code reads `CLAUDE.md`, not `AGENTS.md`, by default, so
this thin pointer keeps a single source of truth while staying compatible.

@AGENTS.md
